{
  "name": "project-f",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "ts-node lib/templating/codegen.ts",
    "todos": "tsc -w -p examples/todos/tsconfig.json",

    "dom": "tsc -w -p lib/dom/tsconfig.json",

    "lab": "ts-node lab/main.ts",
    "macros": "rm -rf lab/macros/transformed/ && ts-node ./lab/macros/transformer.ts && ttsc -P ./lab/macros/tsconfig.json",
    "macros:dev": "ts-node lab/macros/index.ts",

    "test": "mocha -r ts-node/register lib/**/*.spec.ts",
    "test:reactivity": "mocha -r ts-node/register lib/reactivity/**/*.spec.ts",
    "test:dom": "mocha -r jsdom-global/register -r ts-node/register lib/dom/**/*.spec.ts",
    "test:dev": "mocha -r jsdom-global/register -r ts-node/register lib/templating/codegen.spec.ts",

    "build": "npm run clean && tsc -p tsconfig.build.json",
    "ts:ast": "ts-creator lab/_generate.from.ts --use-tabs true --trailing-comma 'all' -o lab/_generate.to.ts",
    "clean": "rm -rf dist",
    "lint": "eslint --fix --ext .ts lib/dom/",
    "preversion": "npm test && npm run build",
    "postversion": "git push origin master && git push origin master --tags && npm publish"
  },
  "devDependencies": {
    "@ts-std/types": "^1.2.0",
    "@types/chai": "^4.2.11",
    "@types/mocha": "^7.0.2",
    "@types/node": "^13.11.0",
    "@typescript-eslint/eslint-plugin": "^2.26.0",
    "@typescript-eslint/parser": "^2.26.0",
    "chai": "^4.2.0",
    "eslint": "^6.8.0",
    "jsdom": "^16.2.2",
    "jsdom-global": "^3.0.2",
    "mocha": "^7.1.1",
    "ts-creator": "^1.2.5",
    "ts-node": "^8.8.2",
    "ttypescript": "^1.5.10",
    "typescript": "^3.8.3"
  }
}
