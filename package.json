{
  "name": "project-f",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "ts-node lab/macros/index.ts",
    "todos": "tsc -w -p examples/todos/tsconfig.json",
    "test": "mocha -r ts-node/register lib/**/*.spec.ts",
    "test:dev": "mocha -r ts-node/register lib/compiler/grammar.spec.ts",
    "clean": "rm -rf dist",
    "dom": "tsc -w -p lib/dom/tsconfig.json",
    "build": "npm run clean && tsc -p tsconfig.build.json",
    "lint": "eslint --fix --ext .ts lib/dom/",
    "ts:ast": "ts-creator lab/_generate.from.ts --use-tabs true --trailing-comma 'all' -o lab/_generate.to.ts",
    "macros": "rm -rf lab/macros/transformed/ && ts-node ./lab/macros/transformer.ts && ttsc -P ./lab/macros/tsconfig.json",
    "preversion": "npm test && npm run build",
    "postversion": "git push origin master && git push origin master --tags && npm publish"
  },
  "devDependencies": {
    "@ts-std/types": "^1.2.0",
    "@types/chai": "^4.2.10",
    "@types/mocha": "^7.0.2",
    "@types/node": "^13.9.0",
    "@typescript-eslint/eslint-plugin": "^2.23.0",
    "@typescript-eslint/parser": "^2.23.0",
    "chai": "^4.2.0",
    "eslint": "^6.8.0",
    "mocha": "^7.1.0",
    "ts-creator": "^1.2.5",
    "ts-node": "^8.6.2",
    "ttypescript": "^1.5.10",
    "typescript": "^3.8.3"
  }
}
