const currentRoute = //

window.addEventListener('hashchange', setStateFromHash, false)
S.cleanup(function () {
	window.removeEventListener('hashchange', setStateFromHash)
})

// todos
	// include done
	// include done and archived
// new todo

export default {
	home() {},
	archived() {},
}
